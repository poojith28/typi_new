# Moved

CIFAR10 active-learning Slurm scripts live under the analysis tree (outside `deep-al`):

```bash
bash /vast/s219110279/analysis/slurm/submit_cifar10_al.sh
```

See `/vast/s219110279/analysis/slurm/` for `cifar10_al_method.sbatch`, `al_env_setup.sh`, and `submit_cifar10_al.sh`.
